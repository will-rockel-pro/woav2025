# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 0
  # maxInstances: 100
  # concurrency: 80
  # cpu: 1
  # memoryMiB: 512

# Environment variables and secrets.
# See https://firebase.google.com/docs/app-hosting/configure
# Note: The 'env' section has been removed to simplify the build process.
# Client-side configuration is handled by Next.js environment variables.
# Server-side functionality now gracefully handles the absence of admin credentials
# during the build step.
